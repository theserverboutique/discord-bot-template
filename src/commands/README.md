# Commands

This folder contains slash commands and message commands used by the bot.

Each command should be stored in its own file to keep the project organised.

Example:

```
ping.js
setup.js
embed.js
help.js
```
